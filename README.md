# homebrew-tap

Pixelite Media Homebrew tap.

## Install

```sh
brew tap pixelitemedia/tap
```

## Formulae

| Formula | Description |
|---------|-------------|
| `gitree` | Bare-repo worktree manager |

```sh
brew install pixelitemedia/tap/gitree
```
